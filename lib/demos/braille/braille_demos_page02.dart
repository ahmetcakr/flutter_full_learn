import 'package:flutter/material.dart';

class BraillePage02 extends StatelessWidget {
  const BraillePage02({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Harf Page'),
      ),
    );
  }
}
