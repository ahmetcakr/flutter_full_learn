import 'package:flutter/material.dart';

class BraillePage04 extends StatelessWidget {
  const BraillePage04({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kelime Page'),
      ),
    );
  }
}
