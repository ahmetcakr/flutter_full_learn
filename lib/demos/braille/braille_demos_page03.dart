import 'package:flutter/material.dart';

class BraillePage03 extends StatelessWidget {
  const BraillePage03({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Deneme Page'),
      ),
    );
  }
}
